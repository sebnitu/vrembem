language: node_js
node_js:
  - 12

before_install:
  - npx lerna bootstrap

before_script:
  - npx del docs/node_modules/@vrembem

script:
  - npm run build
  - npm run lint
  - npm run test:coverage

after_success:
  - npx coveralls < coverage/lcov.info
