{
  "name": "vrembem",
  "version": "0.7.0",
  "private": true,
  "scripts": {
    "css": "lerna run css && lerna run css:min"
  },
  "devDependencies": {
    "del-cli": "^2.0.0",
    "lerna": "^3.15.0",
    "node-sass": "^4.12.0"
  }
}
