<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Vrembem demo catalog</title>
    <link rel="stylesheet" href="<%= site.baseurl %>assets/styles.css" />
  </head>
  <body>
    <h1>This is the hompage content</h1>
    <p>Some stuff will go here...</p>
    <p>But most importantly, here is the menu!</p>

    <ul class="menu menu__stack">
      <% site.menu.forEach(function(item) { %>
        <li class="menu__item">
          <a class="menu__link" href=".<%= item.href %>"><%= item.text %></a>
        </li>
      <% }) %>
    </ul>


  </body>
</html>