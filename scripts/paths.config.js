import path from 'path'

const back = '../'
const paths = {
  src: path.resolve(__dirname, back, 'src'),
  dest: path.resolve(__dirname, back, 'dist'),
  root: path.resolve(__dirname, back)
}

export default paths
