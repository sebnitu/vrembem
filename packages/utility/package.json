{
  "name": "@vrembem/utility",
  "description": "Components that add one off styles or are geared to styling specific html elements and can be extended in other places.",
  "version": "0.0.0",
  "style": "styles.css",
  "scripts": {
    "css": "sass index.scss dist/styles.css --load-path node_modules --source-map",
    "css:min": "sass index.scss dist/styles.min.css --load-path node_modules  --source-map --style compressed",
    "clean": "del dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sebnitu/vrembem.git",
    "directory": "packages/span"
  },
  "dependencies": {
    "@vrembem/core": "^0.0.0"
  }
}
