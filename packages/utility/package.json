{
  "name": "@vrembem/utility",
  "description": "Components that add one off styles or are geared to styling specific html elements and can be extended in other places.",
  "version": "0.0.0",
  "main": "src/utility.js",
  "style": "index.scss",
  "scripts": {
    "build": "npm-run-all clean scripts styles",
    "clean": "del dist",
    "scripts": "npm-run-all scripts:compile scripts:min",
    "scripts:compile": "mkdirp dist && browserify index.js --debug dist/scripts.js -t [ babelify --global --presets [ @babel/preset-env ] ] | exorcist dist/scripts.js.map > dist/scripts.js",
    "scripts:min": "mkdirp dist && browserify index.js --debug dist/scripts.min.js -t [ babelify --global --presets [ @babel/preset-env ] ] -p tinyify | exorcist dist/scripts.min.js.map > dist/scripts.min.js",
    "styles": "npm-run-all styles:compile styles:prefix styles:min",
    "styles:compile": "sass index.scss dist/styles.css --load-path node_modules",
    "styles:min": "sass dist/styles.css dist/styles.min.css --style compressed",
    "styles:prefix": "postcss dist/styles.css --map --use autoprefixer --output dist/styles.css",
    "watch": "npm run watch:scripts watch:styles",
    "watch:scripts": "nodemon -e js -x 'npm run scripts' -i dist",
    "watch:styles": "nodemon -e scss -x 'npm run styles'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sebnitu/vrembem.git",
    "directory": "packages/utility"
  },
  "dependencies": {
    "@vrembem/core": "^0.0.0"
  }
}
