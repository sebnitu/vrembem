{
  "name": "@vrembem/spacing",
  "description": "A component that adds vertical spacing between it's direct children.",
  "version": "0.0.0",
  "style": "styles.css",
  "scripts": {
    "css": "sass index.scss dist/styles.css --load-path node_modules --source-map",
    "css:min": "sass index.scss dist/styles.min.css --load-path node_modules  --source-map --style compressed",
    "clean": "del dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sebnitu/vrembem.git",
    "directory": "packages/spacing"
  },
  "dependencies": {
    "@vrembem/core": "^0.0.0"
  }
}
