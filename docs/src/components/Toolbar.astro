---
import Icon from "@/components/Icon.astro";

interface Props {
  layout: {
    hasAside: boolean;
    hasSidebar: string | undefined;
  };
}

const { layout } = Astro.props;
---

{
  (layout.hasSidebar || layout.hasAside) && (
    <div class="toolbar">
      <div class="clearfix">
        {layout.hasSidebar && (
          <button
            data-drawer-open="layout-sidebar"
            class="button padding-left-sm float-left"
          >
            <Icon name="package" />
            <span class="text-capitalize">{layout.hasSidebar}</span>
          </button>
        )}
        {layout.hasAside && (
          <button
            data-drawer-open="main-aside"
            class="button padding-right-sm float-right"
          >
            <span>On this page</span>
            <Icon name="compass" />
          </button>
        )}
      </div>
    </div>
  )
}
