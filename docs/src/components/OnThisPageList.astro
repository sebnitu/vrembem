---
const { item } = Astro.props;
---

<li class="otp__item">
  <a class="otp__link" href={"#" + item.slug}>
    {item.text}
  </a>
  {
    item.subheadings.length > 0 && (
      <ul class="otp__sublist">
        {item.subheadings.map((subheading) => (
          <Astro.self item={subheading} />
        ))}
      </ul>
    )
  }
</li>
