---
title: Checkbox
description: "Checkboxes allow the user to select multiple options from a set."
package: "@vrembem/checkbox"
category: form-control
---

import ReferenceTable from "@/components/ReferenceTable.astro";

```sh
npm install @vrembem/checkbox
```

<CodeExample lang="scss">
  ```scss
  @use "@vrembem/checkbox";
  ```
</CodeExample>

## Options

Checkbox is built as a Sass module. Sass options are used to control CSS output, while design token CSS variables control the styling of the component. Below are the available configuration options.

### Sass

Checkbox comes with the following Sass configuration options that can be set using either the [config module](/reference/sass-modules/config#using-config-module) or [Sass `with` clause](/reference/sass-modules/config#using-with-clause) methods.

<CodeExample lang="scss">
```scss
@use "@vrembem/checkbox" with (
  $config: (
    // Map for outputting checkbox_size_[key] modifier variants
    // @type map
    "checkbox-size": (
      "breakpoints": true,
      "default": tokens.get("form-control-size"),
      "sm": tokens.get("form-control-size-sm"),
      "md": tokens.get("form-control-size"),
      "lg": tokens.get("form-control-size-lg")
    )
  )
);
```
</CodeExample>

### Design tokens

Checkbox uses design token CSS variables in a reference and fallback pattern. Variables are referenced in the component's styles but are not directly defined; instead, they are provided with sensible fallbacks.

<ReferenceTable id="tokens-checkbox" valueLabel="Fallback" expandable={true} data={{
  "--vb-checkbox-color": "var(--vb-form-control-choice-color)",
  "--vb-checkbox-accent": "var(--vb-form-control-choice-color-checked)",
  "--vb-checkbox-fill": "var(--vb-form-control-choice-fill)",
  "--vb-checkbox-size": "var(--vb-form-control-size)",

  "--vb-checkbox-background-opacity": "var(--vb-form-control-choice-background-opacity)",
  "--vb-checkbox-background-opacity-hover": "var(--vb-form-control-choice-background-opacity-hover)",
  "--vb-checkbox-background-opacity-focus": "var(--vb-form-control-choice-background-opacity-focus)",
  "--vb-checkbox-background-opacity-active": "var(--vb-form-control-choice-background-opacity-active)",
  "--vb-checkbox-background-border-radius": "var(--vb-form-control-choice-background-border-radius)",
  "--vb-checkbox-opacity-disabled": "var(--vb-form-control-opacity-disabled)",

  "--vb-checkbox-transition-duration": "var(--vb-form-control-transition-duration)",
  "--vb-checkbox-transition-timing-function": "var(--vb-form-control-transition-timing-function)",

  "--vb-checkbox-box-size": "1.2em",
  "--vb-checkbox-border-width": "2px",
  "--vb-checkbox-border-radius": "var(--vb-border-radius)",
  "--vb-checkbox-icon-size": "0.8em",
}} />

## Basic usage

Checkbox form controls are composed using the `<input type="checkbox">` element along with the `.checkbox` class.

<CodeExample tabs>
  <div slot="output" class="flex">
    <input class="checkbox" type="checkbox" checked />
    <input class="checkbox" type="checkbox" aria-checked="mixed" />
    <input class="checkbox" type="checkbox" />
  </div>
  <TabsPanel label="HTML">
    ```html
    <input class="checkbox" type="checkbox" />
    ```
  </TabsPanel>
  <TabsPanel label="JS">
    ```js
    // Get checkboxes with "mixed" status
    const checkboxes = document.querySelectorAll(
      "input[type='checkbox'][aria-checked='mixed']"
    );

    // Apply the indeterminate state
    checkboxes.forEach((checkbox) => checkbox.indeterminate = true);
    ```
  </TabsPanel>
</CodeExample>

Indeterminate checkboxes need to be set in JavaScript though styling is provided by Vrembem for the indeterminate states (see example above). Review [MDN documentation](https://developer.mozilla.org/en-US/docs/Web/CSS/:indeterminate) for more details.

### Labels

For checkboxes with labels, wrap the checkbox element along with the text label using the `<label>` element.

<CodeExample>
  <Fragment slot="output">
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <input class="checkbox" type="checkbox" checked />
      <span>Checkbox with a label</span>
    </label>
    <br />
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <input class="checkbox" type="checkbox" aria-checked="mixed" />
      <span>Checkbox with a label</span>
    </label>
    <br />
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <input class="checkbox" type="checkbox" />
      <span>Checkbox with a label</span>
    </label>
  </Fragment>
  ```html
  <label>
    <input class="checkbox" type="checkbox" />
    <span>Checkbox with a label</span>
  </label>
  ```
</CodeExample>

### Disabled

Adding the boolean `disabled` attribute to the input element will provide visual indication that the checkbox is not available for use.

<CodeExample>
  <Fragment slot="output">
    <div class="flex">
      <input class="checkbox" type="checkbox" checked disabled />
      <input class="checkbox" type="checkbox" disabled />
    </div>
    <br />
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <input class="checkbox" type="checkbox" checked disabled />
      <span>Checkbox with a label</span>
    </label>
    <br />
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <input class="checkbox" type="checkbox" disabled />
      <span>Checkbox with a label</span>
    </label>
  </Fragment>
  ```html
  <input class="checkbox" type="checkbox" disabled />
  ```
</CodeExample>

## checkbox_size_[key]

Adjust the size of a checkbox by increasing or decreasing its font-size. These modifiers are generated using key/value pairs of the `checkbox-size` config options map.

<CodeExample>
  <div slot="output" class="flex align-items-center">
    <input class="checkbox checkbox_size_sm" type="checkbox" />
    <input class="checkbox" type="checkbox" />
    <input class="checkbox checkbox_size_lg" type="checkbox" />
  </div>
  ```html
  <input class="checkbox checkbox_size_sm" type="checkbox" />
  <input class="checkbox checkbox_size_lg" type="checkbox" />
  ```
</CodeExample>

**Available variants**

- `checkbox_size_sm`
- `checkbox_size_lg`

### Breakpoint variants

These modifiers come with media breakpoint variants. This allows changing styles based on a specific breakpoint key in the [`breakpoints`](/packages/core#config-options) config options map. Breakpoint variants output can be toggled using the `breakpoints` key in the `checkbox-size` config options map (default: `true`).

<CodeExample>
  <div slot="output" class="spacing">
    <p>Current view-port width: <code data-vp-width>...</code></p>
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <input class="checkbox checkbox_size_sm lg:checkbox_size_md xl:checkbox_size_lg" type="checkbox" checked />
      <span>Checkbox</span>
      <span class="badge lg:display-none">size_sm</span>
      <span class="badge display-none lg:display-block xl:display-none">size_md</span>
      <span class="badge display-none xl:display-block">size_lg</span>
    </label>
  </div>
  ```html
  <input class="checkbox checkbox_size_sm lg:checkbox_size_md xl:checkbox_size_lg" type="checkbox" />
  ```
</CodeExample>

**Available variants**

- `xs:checkbox_size_[key]`
- `sm:checkbox_size_[key]`
- `md:checkbox_size_[key]`
- `lg:checkbox_size_[key]`
- `xl:checkbox_size_[key]`
