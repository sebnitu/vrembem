---
title: Drawer
description: "A responsive container component that switches between a static and a modal dialog based on a provided breakpoint."
package: "@vrembem/drawer"
category: component
---

import ReferenceTable from "@/components/ReferenceTable.astro";

```sh
npm install @vrembem/drawer
```

<CodeExample lang="scss">
  ```scss
  @use "@vrembem/drawer";
  ```
</CodeExample>

<CodeExample lang="js">
  ```js
  // Import the drawer collection class
  import { DrawerCollection } from "@vrembem/drawer";

  // Instantiate the drawer collection
  const drawers = new DrawerCollection();

  // Mount the drawer collection
  await drawers.mount();
  ```
</CodeExample>

## Options

Drawer is built as a Sass module to handle styles and CSS output, along with a corresponding JavaScript class for managing behavior and plugin integration. Below are the available configuration options.

### JavaScript

TBD...

<CodeExample lang="js">
```js
static get observedAttributes() {
  return ["breakpoint", "position"];
}
```
</CodeExample>

### Sass

Drawer comes with the following Sass configuration options that can be set using either the [config module](/reference/sass-modules/config#using-config-module) or [Sass `with` clause](/reference/sass-modules/config#using-with-clause) methods.

<CodeExample lang="scss">
```scss
@use "@vrembem/drawer" with (
  $config: (
    // Sets the default breakpoint for drawers
    // @type string | number
    "drawer-breakpoint": "md",

    // Set the default position of modal drawers
    // @type string
    "drawer-position": "left"
  )
);
```
</CodeExample>

### Design tokens

Drawer uses design token CSS variables in a reference and fallback pattern. Variables are referenced in the component's styles but are not directly defined; instead, they are provided with sensible fallbacks.

<ReferenceTable id="tokens-drawer" filter={false} data={{
  "--vb-drawer-breakpoint": "md",
  "--vb-drawer-position": "left"
}} />

## Basic usage

TBD...
