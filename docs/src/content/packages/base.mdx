---
title: Base
description: "A collection of modules that apply global style to commonly used HTML elements."
package: "@vrembem/base"
status: "layers"
---

import ReferenceBar from "../../components/ReferenceBar.astro";
import ReferenceCard from "../../components/ReferenceCard.astro";
import ReferenceTable from "../../components/ReferenceTable.astro";
import data from "../../data/cssvar-base.json";

# Base

A collection of modules that apply global style to commonly used HTML elements.

```sh
npm install @vrembem/base
```

<CodeExample lang="scss">
  ```scss
  @use "@vrembem/base";
  ```
</CodeExample>

## Output

Base modules each have their own output styles. These are all output by default when the base package is loaded. There are two ways to control which modules are output using the `$output` variable and `$output-exceptions` list.

<ReferenceCard name="$output" type="boolean" defaults="true">
  Controls whether module styles are output. You can specify exceptions using the `$output-exceptions` list.
</ReferenceCard>

<ReferenceCard name="$output-exceptions" type="list" defaults="()">
  Modules listed in this variable are exceptions to the value set in `$output`. If `$output` is `true`, modules listed here will not be output. If `$output` is `false`, modules listed here will be output.
</ReferenceCard>

#### Example

Use the `$output` variable to toggle output for all modules. Add exceptions to the `$output-exceptions` list to override this setting for specific modules.

<CodeExample lang="scss">
```scss
// Disable the output of specific modules
@use "@vrembem/base" with (
  $output-exceptions: (
    "blockquote",
    "code",
    "list",
    "pre"
  )
);

// Disable the output of all modules except for "normalize"
@use "@vrembem/base" with (
  $output: false,
  $output-exceptions: (
    "normalize"
  )
);
```
</CodeExample>

## Basic usage

Once loaded, the base component provides default styles for HTML elements and a number of output modules. To style HTML elements, use their corresponding classes or apply the `type` class to style an entire block of content.

<CodeExample lang="html">
```html
<!-- Using base element modules -->
<h1 class="h1">...</h1>
<p>...</p>
<ul class="list">
  <li>...</li>
</ul>

<!-- Using base type module -->
<div class="type">
  <h1>...</h1>
  <p>...</p>
  <ul>
    <li>...</li>
  </ul>
</div>
```
</CodeExample>

You can also customize the class selectors used by modules by mapping the module name to your preferred class selector in the `$classes` map.

<ReferenceCard name="$classes" type="map" defaults='("separator": "sep")'>
  A map containing module names as keys and their preferred class selectors as values. For example, to use the class `.content` instead of `.type`, you could set the following:

```scss
@use "@vrembem/base" with (
  $classes: (
    "separator": "sep",
    "type": "content"
  )
);
```
</ReferenceCard>

## Modules

Below is a list of each base module, a description of what they do, their default class selectors (if any), and how to use them in your project.

### normalize

<ReferenceBar selector="none" hash={{
  target: "reference-table-cssvar-base",
  event: "filter",
  detail: {
    value: "--vb-base"
  }
}}>
  Variables
</ReferenceBar>

A module that helps ensure HTML elements are rendered consistently across browsers by resetting and standardizing their default styles. This provides a reliable foundation for building components, making elements more predictable and easier to work with by:

- Resetting margin, padding, and box-sizing for all elements.
- Setting default html/body height, font, and background styles.
- Normalizing heading, image, table and caption elements.
- Creating consistent typography styles for code and pre elements.
- Normalizing form-control element appearance and font styles.

#### Variables

The following CSS variables are referenced with a provided fallback for more easily overriding base properties:

<ReferenceCard name="--vb-base-height" type="height value" fallback="100%">
  Used to set the height property on both the `html` and `body` elements.
</ReferenceCard>

<ReferenceCard name="--vb-base-background" type="color" fallback="var(--vb-background)">
  Used to set the background property on the `body` element.
</ReferenceCard>

<ReferenceCard name="--vb-base-foreground" type="color" fallback="var(--vb-foreground)">
  Used to set the color property on the `body` element.
</ReferenceCard>

<ReferenceCard name="--vb-base-font-size" type="font-size value" fallback="16px">
  Used to set the font-size property on the `html` element.
</ReferenceCard>

<ReferenceCard name="--vb-base-line-height" type="line-height value" fallback="var(--vb-line-height)">
  Used to set the line-height property on the `html` element.
</ReferenceCard>

### blockquote

<ReferenceBar selector=".blockquote" hash={{
  target: "reference-table-cssvar-base",
  event: "filter",
  detail: {
    value: "--vb-blockquote"
  }
}}>
  Variables
</ReferenceBar>

The HTML blockquote element is used for marking up extended quotations. This module helps style these elements in a distinct and appealing way.

<CodeExample lang="html">
<Fragment slot="output">
  <blockquote class="blockquote">
    <p>"All that is gold does not glitter, Not all those who wander are lost; The old that is strong does not wither, Deep roots are not reached by the frost."</p>
    <p>Appears in <cite>"The Fellowship of the Ring"</cite> by J.R.R. Tolkien</p>
  </blockquote>
</Fragment>
```html
<blockquote class="blockquote" cite="...">
  <p>...</p>
  <footer>
    ...
    <cite>...</cite>
  </footer>
</blockquote>
```
</CodeExample>

### code

<ReferenceBar selector=".code" hash={{
  target: "reference-table-cssvar-base",
  event: "filter",
  detail: {
    value: "--vb-code"
  }
}}>
  Variables
</ReferenceBar>

The HTML code element displays its contents styled in a fashion intended to indicate that the text is a short fragment of computer code.

<CodeExample lang="html">
<Fragment slot="output">
  <code class="code">a = 17</code>
</Fragment>
```html
<code class="code">a = 17</code>
```
</CodeExample>

### heading

Section headings in HTML are represented by the `<h1>` through `<h6>` elements. This module helps style these elements by providing the `.h1`-`.h6` CSS classes.

<CodeExample lang="html">
<Fragment slot="output">
  <div class="spacing">
    <h1 class="h1">Heading h1</h1>
    <h2 class="h2">Heading h2</h2>
    <h3 class="h3">Heading h3</h3>
    <h4 class="h4">Heading h4</h4>
    <h5 class="h5">Heading h5</h5>
    <h6 class="h6">Heading h6</h6>
  </div>
</Fragment>
```html
<h1 class="h1">...</h1>
<h2 class="h2">...</h2>
<h3 class="h3">...</h3>
<h4 class="h4">...</h4>
<h5 class="h5">...</h5>
<h6 class="h6">...</h6>
```
</CodeExample>

{/*
.h1,
.h2,
.h3,
.h4,
.h5,
.h6 {
  color: var(--vb-heading-color, var(--vb-foreground));
  font-family: var(--vb-heading-font-family, inherit);
  font-weight: var(--vb-heading-font-weight, var(--vb-font-weight-semi-bold));
  line-height: var(--vb-heading-line-height);
}
*/}

#### Heading scale

A map containing the font-size and optional line-height scale for HTML headings. The map should contain a key of the heading level and the value of a font-size and optional line-height space separated.

```scss
$heading-scale: (
  "h1": 2.75em,
  "h2": 2em,
  "h3": 1.75em,
  "h4": 1.5em,
  "h5": 1.25em inherit,
  "h6": 1em inherit
) !default;
```

### link

A link—usually represented by an anchor (`<a>`) HTML element with `href` attribute—creates the styles for a hyperlink to anything a URL can address. This module helps style these elements by providing the `.link` CSS class as well as a few optional modifiers.

```html
<a href="#" class="link">Link</a>
<a href="#" class="link link_subtle">Link</a>
<a href="#" class="link link_invert">Link</a>
<a href="#" class="link link_invert-subtle">Link</a>
```

{/*
.link, .type :not(div) > a {
  color: var(--vb-link-color, var(--vb-primary-50));
  text-decoration: var(--vb-link-decoration, underline);
  text-decoration-thickness: var(--vb-link-decoration, var(--vb-link-thickness, 1px));
  text-underline-offset: var(--vb-link-underline, var(--vb-link-offset, 2px));
}
.link code, .type :not(div) > a code {
  background-color: hsl(var(--vb-primary-hs) 50% / 10%);
  color: var(--vb-link-color, var(--vb-primary-50));
}
.link:hover, .type :not(div) > a:hover {
  color: var(--vb-link-color, var(--vb-link-hover, var(--vb-primary-40)));
  text-decoration: var(--vb-link-decoration, var(--vb-link-hover, none));
}
.link:hover code, .type :not(div) > a:hover code {
  color: var(--vb-link-color, var(--vb-link-hover, var(--vb-primary-40)));
}
.link:focus, .type :not(div) > a:focus {
  color: var(--vb-link-color, var(--vb-link-hover, var(--vb-primary-40)));
  outline: var(--vb-link-focus-ring, currentcolor dotted 1px);
  outline-offset: var(--vb-link-focus-ring-offset, 0.125rem);
  text-decoration: var(--vb-link-decoration, var(--vb-link-hover, none));
}
.link:focus code, .type :not(div) > a:focus code {
  color: var(--vb-link-color, var(--vb-link-hover, var(--vb-primary-40)));
}
*/}

### list

The list module helps add styles to unordered (`<ul>`) and ordered (`<ol>`) lists by providing the `.list` class.

```html
<ul class="list">
  <li>One</li>
  <li>Two
    <ul>
      <li>One</li>
      <li>Two</li>
      <li>Three</li>
    </ul>
  </li>
  <li>Three</li>
</ul>

<ol class="list">
  ...
</ol>
```

{/*
.list, .type > ul, .type > ol {
  margin-left: var(--vb-list-indent, 1.5em);
}
.list ul,
.list ol, .type > ul ul,
.type > ul ol, .type > ol ul,
.type > ol ol {
  margin-left: var(--vb-list-indent, 1.5em);
}
.list li li,
.list li + li, .type > ul li li,
.type > ul li + li, .type > ol li li,
.type > ol li + li {
  margin-top: var(--vb-list-gap, 0.5em);
}

.list-style-none {
  list-style: none !important;
}
*/}

### pre

This module helps style the HTML `<pre>` element by providing the `.pre` CSS class. Whitespace inside this element is displayed as written.

```html
<pre class="pre">
-----------------------------
| I'm an expert in my field |
-----------------------------
     \   ^__^
      \  (oo)\_______
         (__)\       )\/\
             ||----w |
             ||     ||
</pre>
```

{/*
.pre, .type > pre {
  background: var(--vb-pre-background, var(--vb-background-dark));
  border: var(--vb-pre-padding);
  border-radius: var(--vb-pre-padding, var(--vb-border-radius));
  color: var(--vb-pre-foreground, var(--vb-foreground));
  font-family: var(--vb-font-family-mono);
  overflow: auto;
  padding: var(--vb-pre-padding, 1em);
}
.pre code, .type > pre code {
  background: none;
  border: none;
  color: inherit;
  font-size: var(--vb-pre-font-size, var(--vb-font-size-sm));
  padding: 0;
}
*/}

### separator

This module adds the `.sep` and `.sep-invert` CSS classes which visually renders a horizontal separator. This can be applied to an `<hr>` HTML element. It can also be applied to a more generic `<span>` or `<div>` depending on the semantic context.

```html
<!-- Separator -->
<hr class="sep">
<hr class="sep border-color-darker">

<!-- Invert separator -->
<hr class="sep-invert">
<hr class="sep-invert border-color-invert-darker">
```

{/*
.sep, .type > hr {
  border: none;
  border-top: var(--vb-separator-border, var(--vb-border));
  display: block;
  height: 0;
}
*/}

### type

The type module provides the `.type` CSS class as a quick way to apply many base modules to HTML elements directly. Base module classes will override a parent `.type` application when explicitly set. Use `.type_invert` for when text sits on a dark background.

```html
<div class="type">
  <h1>Heading</h1>
  <p>...</p>
  <ul>
    <li>...</li>
    <li>...</li>
    <li>...</li>
  </ul>
</div>
```

{/*
.type {
  color: var(--vb-type-color, var(--vb-foreground-light));
  font-family: var(--vb-type-font-family);
  font-size: var(--vb-type-font-size, var(--vb-font-size-lg));
  line-height: var(--vb-type-line-height, 1.7778);
}
.type > * + * {
  margin-top: var(--vb-type-gap);
}
*/}

Modules that get mapped to HTML elements include:

- `.h1`-`.h6` &rarr; `<h1>`-`<h6>`
- `.link` &rarr; `<a>`
- `.code` &rarr; `<code>`
- `.sep` &rarr; `<hr>`
- `.list` &rarr; `<ul>` `<ol>`
- `.blockquote` &rarr; `<blockquote>`
- `.pre` &rarr; `<pre>`

> The type module only applies styles to children HTML elements. It's possible to nest other components within type and not have style or specificity conflicts. It's recommended to wrap nested components with anonymous `<div>` elements when possible.

### arrow

The arrow (caret) module creates directional triangles drawn with CSS.

<CodeExample>
  <div slot="output" class="flex">
    <span class="arrow"></span>
    <span class="arrow-up"></span>
    <span class="arrow-left"></span>
    <span class="arrow-right"></span>
  </div>
  ```html
  <span class="arrow"></span>
  <span class="arrow-up"></span>
  <span class="arrow-left"></span>
  <span class="arrow-right"></span>
  ```
</CodeExample>

{/*
.arrow,
.arrow-up,
.arrow-down,
.arrow-left,
.arrow-right {
  border-width: var(--vb-arrow-size, 6px 4px);
  border-bottom-width: 0;
  border-color: var(--vb-arrow-color, currentcolor) transparent transparent;
  border-radius: var(--vb-arrow-radius, 2px);
  border-style: solid;
  display: inline-block;
  flex-grow: 0;
  flex-shrink: 0;
  height: 0;
  pointer-events: none;
  transform-origin: center;
  width: 0;
}

.arrow-up {
  transform: rotate(180deg);
}

.arrow-left {
  transform: rotate(90deg);
}

.arrow-right {
  transform: rotate(-90deg);
}
*/}

#### Example

Arrows are great indicators for buttons and menu items when interacting with them would toggle a popover or other togglable components.

```html
<button class="button">
  <span>Button</span>
  <span class="arrow"></span>
</button>
```

### loading-spinner

...

{/*
.loading-spinner {
  height: var(--vb-loading-size, 1em);
  width: var(--vb-loading-size, 1em);
  animation: spin var(--vb-loading-duration, 0.6s) infinite linear;
  border: var(--vb-loading-border, 2px solid);
  border-color: var(--vb-loading-color, currentcolor) var(--vb-loading-color, currentcolor) transparent transparent;
  border-radius: 9999px;
  display: inline-block;
  position: relative;
}
@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}
*/}

### scroll-box

This module provides the `.scroll-box` CSS class to be applied as a wrapper around elements when they need a scrollable container.

```html
<div class="scroll-box">
  ...
</div>
```

{/*
.scroll-box {
  display: block;
  overflow: auto;
  -webkit-overflow-scrolling: touch;
  width: 100%;
}
*/}

### sr-only

...

{/*
.sr-only {
  clip-path: inset(50%);
  height: 1px;
  margin: -1px;
  overflow: hidden;
  padding: 0;
  position: absolute;
  width: 1px;
}
*/}

## Reference

Below is a reference to all CSS variables that are referenced by `base` modules along with their fallback values. These variables are only referenced (not defined), allowing you to easily customize styles by setting your own values as needed.

{/* TODO: Add ability to link values to csscvar-core.json reference table */}
<ReferenceTable id="cssvar-base" data={data} valueLabel="Fallback" />
