---
title: CSS Variables
description: "Core custom properties for establishing a consistent design language across components."
parent: core
group: reference
---

import ReferenceTable from "../../../components/ReferenceTable.astro";
import data from "../../../data/cssvar-core.json";

<CodeExample lang="scss">
```scss
@forward "@vrembem/core/root";
```
</CodeExample>

Below is a reference of all the `core` CSS custom properties and their value. By default, custom properties are not output by the core package.

<ReferenceTable data={data} />
