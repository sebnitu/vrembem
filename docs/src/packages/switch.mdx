---
title: Switch
description: "Switches are a binary form element used to toggle between two options."
package: "@vrembem/switch"
category: form-control
---

import ReferenceTable from "../components/ReferenceTable.astro";

```sh
npm install @vrembem/switch
```

<CodeExample lang="scss">
  ```scss
  @use "@vrembem/switch";
  ```
</CodeExample>

## Options

Switch is built as a Sass module. Sass options are used to control CSS output, while CSS variables control the styling of the component. Below are the available configuration options.

### Sass

Switch comes with the following Sass configuration options that can be set using either the [config module](/packages/core/config#using-config-module) or [Sass `with` clause](/packages/core/config#using-with-clause) methods.

<CodeExample lang="scss">
```scss
@use "@vrembem/switch" with (
  $config: (
    // Map for outputting switch_size_[key] modifier variants
    // @type map
    "switch-size": (
      "breakpoints": true,
      "default": tokens.get("form-control-size"),
      "sm": tokens.get("form-control-size-sm"),
      "md": tokens.get("form-control-size"),
      "lg": tokens.get("form-control-size-lg")
    )
  )
);
```
</CodeExample>

### CSS Variables

Switch uses CSS variables in a reference and fallback pattern. Variables are referenced in the component's styles but are not directly defined; instead, they are provided with sensible fallbacks.

<ReferenceTable id="cssvar-switch" valueLabel="Fallback" expandable={true} data={{
  "--vb-switch-color": "var(--vb-form-control-choice-color)",
  "--vb-switch-color-checked": "var(--vb-form-control-choice-color-checked)",
  "--vb-switch-fill": "var(--vb-form-control-choice-fill)",
  "--vb-switch-size": "var(--vb-form-control-size)",

  "--vb-switch-background-opacity": "var(--vb-form-control-choice-background-opacity)",
  "--vb-switch-background-opacity-hover": "var(--vb-form-control-choice-background-opacity-hover)",
  "--vb-switch-background-opacity-focus": "var(--vb-form-control-choice-background-opacity-focus)",
  "--vb-switch-background-opacity-active": "var(--vb-form-control-choice-background-opacity-active)",
  "--vb-switch-background-border-radius": "var(--vb-form-control-choice-background-border-radius)",
  "--vb-switch-opacity-disabled": "var(--vb-form-control-opacity-disabled)",

  "--vb-switch-transition-duration": "var(--vb-form-control-transition-duration)",
  "--vb-switch-transition-timing-function": "var(--vb-form-control-transition-timing-function)",

  "--vb-switch-track-size": "1.25em",
  "--vb-switch-border-width": "2px",
  "--vb-switch-border-radius": "var(--vb-border-radius-circle)",
}} />

## Basic usage

Switch form controls are composed using a set of `<span>` elements alongside the native `<input type="checkbox">` element which should be given the `switch__native` class and come before the remaining presentational `<span>` elements.

<CodeExample>
  <Fragment slot="output">
    <span class="switch">
      <input class="switch__native" type="checkbox"  checked />
      <span class="switch__background">
        <span class="switch__track">
          <span class="switch__thumb"></span>
        </span>
      </span>
    </span>
    <span class="switch">
      <input class="switch__native" type="checkbox"  />
      <span class="switch__background">
        <span class="switch__track">
          <span class="switch__thumb"></span>
        </span>
      </span>
    </span>
  </Fragment>
  ```html
  <span class="switch">
    <input class="switch__native" type="checkbox">
    <span class="switch__background">
      <span class="switch__track">
        <span class="switch__thumb"></span>
      </span>
    </span>
  </span>
  ```
</CodeExample>

### Labels

For switch with labels, just wrap the switch component along with label text using the `<label>` element.

<CodeExample>
  <Fragment slot="output">
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <span class="switch">
        <input class="switch__native" type="checkbox"  checked />
        <span class="switch__background">
          <span class="switch__track">
            <span class="switch__thumb"></span>
          </span>
        </span>
      </span>
      <span>Switch with a label</span>
    </label>
    <br />
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <span class="switch">
        <input class="switch__native" type="checkbox"  />
        <span class="switch__background">
          <span class="switch__track">
            <span class="switch__thumb"></span>
          </span>
        </span>
      </span>
      <span>Switch with a label</span>
    </label>
    <br />
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <span class="switch">
        <input class="switch__native" type="checkbox"  />
        <span class="switch__background">
          <span class="switch__track">
            <span class="switch__thumb"></span>
          </span>
        </span>
      </span>
      <span>Switch with a label</span>
    </label>
  </Fragment>
  ```html
  <label>
    <span class="switch">
      ...
    </span>
    <span>Switch with a label</span>
  </label>
  ```
</CodeExample>

### Disabled

Adding the boolean `disabled` attribute to the input element will provide visual indication that the switch is not available for use.

<CodeExample>
  <Fragment slot="output">
    <span class="switch">
      <input class="switch__native" type="checkbox"  checked disabled />
      <span class="switch__background">
        <span class="switch__track">
          <span class="switch__thumb"></span>
        </span>
      </span>
    </span>
    <span class="switch">
      <input class="switch__native" type="checkbox"  disabled />
      <span class="switch__background">
        <span class="switch__track">
          <span class="switch__thumb"></span>
        </span>
      </span>
    </span>
    <br />
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <span class="switch">
        <input class="switch__native" type="checkbox"  disabled checked />
        <span class="switch__background">
          <span class="switch__track">
            <span class="switch__thumb"></span>
          </span>
        </span>
      </span>
      <span>Switch with a label</span>
    </label>
    <br />
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <span class="switch">
        <input class="switch__native" type="checkbox"  disabled />
        <span class="switch__background">
          <span class="switch__track">
            <span class="switch__thumb"></span>
          </span>
        </span>
      </span>
      <span>Switch with a label</span>
    </label>
  </Fragment>
  ```html
  <span class="switch">
    <input class="switch__native" type="checkbox"  disabled />
    <span class="switch__background">
      <span class="switch__track">
        <span class="switch__thumb"></span>
      </span>
    </span>
  </span>
  ```
</CodeExample>

## switch_size_[value]

Adjust the size of a switch by increasing or decreasing its font-size. These modifiers are generated using key/value pairs of the `switch-size` config options map.

<CodeExample>
  <Fragment slot="output">
    <span class="switch switch_size_sm">
      <input class="switch__native" type="checkbox"  />
      <span class="switch__background">
        <span class="switch__track">
          <span class="switch__thumb"></span>
        </span>
      </span>
    </span>
    <span class="switch">
      <input class="switch__native" type="checkbox"  />
      <span class="switch__background">
        <span class="switch__track">
          <span class="switch__thumb"></span>
        </span>
      </span>
    </span>
    <span class="switch switch_size_lg">
      <input class="switch__native" type="checkbox"  />
      <span class="switch__background">
        <span class="switch__track">
          <span class="switch__thumb"></span>
        </span>
      </span>
    </span>
  </Fragment>
  ```html
  <span class="switch switch_size_sm">
    ...
  </span>

  <span class="switch switch_size_lg">
    ...
  </span>
  ```
</CodeExample>

**Available variants**

- `switch_size_sm`
- `switch_size_lg`

### Breakpoint variants

These modifiers come with media breakpoint variants. This allows changing styles based on a specific breakpoint key in the [`breakpoints`](/packages/core#config-options) config options map. Breakpoint variants output can be toggled using the `breakpoints` key in the `switch-size` config options map (default: `true`).

<CodeExample>
  <div slot="output" class="spacing">
    <p>Current view-port width: <code data-vp-width>...</code></p>
    <label class="flex flex_inline flex_gap_sm align-items-center">
      <span class="switch switch_size_sm lg:switch_size_md xl:switch_size_lg">
        <input class="switch__native" type="checkbox"  />
        <span class="switch__background">
          <span class="switch__track">
            <span class="switch__thumb"></span>
          </span>
        </span>
      </span>
      <span>Switch</span>
      <span class="badge lg:display-none">size_sm</span>
      <span class="badge display-none lg:display-block xl:display-none">size_md</span>
      <span class="badge display-none xl:display-block">size_lg</span>
    </label>
  </div>
  ```html
  <span class="switch switch_size_sm lg:switch_size_md xl:switch_size_lg">
    ...
  </span>
  ```
</CodeExample>

**Available variants**

- `xs:switch_size_[key]`
- `sm:switch_size_[key]`
- `md:switch_size_[key]`
- `lg:switch_size_[key]`
- `xl:switch_size_[key]`
