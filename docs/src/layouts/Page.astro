---
import Base from "./Base.astro";
import Navibar from "../components/Navibar.astro";
import Footer from "../components/Footer.astro";
import LayoutDrawer from "../components/LayoutDrawer.vue";

interface Props {
  title?: string;
  drawer?: boolean;
}

const { title, drawer = true } = Astro.props;
---

<Base title={title}>
  <Navibar />
  <div class={`layout${drawer ? " has-drawer" : ""}`}>
    {
      drawer && (
        <LayoutDrawer client:load>
          <slot name="drawer" />
        </LayoutDrawer>
      )
    }
    <div class="layout__content">
      <div class="content">
        <div class="content__container">
          <div class="gap-y">
            <h2>On this page</h2>
            <slot name="aside" />
          </div>
          <div class="type gap-y">
            <button
              data-drawer-open="layout-drawer"
              class="link layout-drawer-open"
            >
              Menu</button
            >
            <slot />
          </div>
          <Footer classes="border-top padding-top-xl margin-top-xl" />
        </div>
      </div>
    </div>
  </div>
</Base>
