---
import Base from './Base.astro';

const { frontmatter } = Astro.props;
---

<Base title={frontmatter.title}>
  <main class="section">
		<div class="section__container max-width-xs type gap-y">
      <slot />
    </div>
  </main>
</Base>
